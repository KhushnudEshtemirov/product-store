import React from "react";

import "../../styles/styles.scss";

const Loading = () => {
  return (
    <section>
      <span className="loader"> </span>
    </section>
  );
};

export default Loading;
